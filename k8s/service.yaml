apiVersion: v1
kind: Service
metadata:
  name: appddata-admin
  namespace: appddata
  labels:
    app: appddata-admin
spec:
  type: NodePort

  selector:
    app: appddata-admin

  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
